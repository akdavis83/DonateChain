{
  "name": "covenant-pay-contracts",
  "version": "1.0.0",
  "description": "CovenantPay Donation Protocol - RSK Testnet",
  "scripts": {
    "compile": "npx hardhat compile",
    "extract-abi": "npx hardhat run scripts/extract-abi.js",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network rskTestnet",
    "verify:testnet": "npx hardhat verify --network rskTestnet",
    "test": "npx hardhat test",
    "coverage": "npx hardhat coverage"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "hardhat": "^2.22.0",
    "solidity-coverage": "^0.8.12"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  }
}
